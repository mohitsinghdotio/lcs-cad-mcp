"""Layers module — register(mcp) wires all layer tools into the FastMCP instance."""
from fastmcp import FastMCP


def register(mcp: FastMCP) -> None:
    """Register all layer tools. Called once by __main__.py at startup."""
    pass  # Tools added in Epic 3 stories
